import React from "react";
import "./Spinner.scss";

const Spinner = () => {
  return (
    <div className="spinner">
      <i className="fas fa-spinner fa-2x" />
    </div>
  );
};

export default Spinner;
